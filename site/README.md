# ZK Compiler Research

Static Leptos CSR site for the ZKC.

## Development

```bash
cd site
./scripts/trunk.sh serve
```

## Build

```bash
cd site
./scripts/trunk.sh build --release
```

The production output is written to `site/dist/`.
